apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spring-boot3-monolith-ingress
spec:
  defaultBackend:
    service:
      name: spring-boot3-monolith-service
      port:
        number: 8080
